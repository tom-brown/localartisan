<!DOCTYPE html>
<html>
<head>
  <title>Handmadelocal</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="container">
    <div id="content">
      <div id="topbar">
        
        <a class="brand"><%= link_to image_tag("logo.jpg"), root_url %></a> 

        <% if logged_in? %>
          <div id="pull-right">            
            <%=link_to "Browse", items_url%> -
            <%=link_to "Sell", new_item_url%> -
            <%=link_to "Favorites", favorites_url%> -
            <%=link_to "My Account", user_url(current_user) %> -
            <%=link_to "Sign Out", logout_url, :method => :delete%>
          </div>
        <% else %>
          <div id="pull-right">
            <%=form_tag login_url do %>
              <%=text_field_tag :email, nil, :placeholder => "Email" %>
              <%=password_field_tag :password, nil, :placeholder => "Password" %>
              <%=submit_tag "Go!"%>
            <% end %>
            <br />            
          </div>
          <div id="sign_in"> <!-- <button type="button" name="" value="" class="css3button">submit</button> -->
            <%= link_to "Sign-Up!", new_user_url %> or Sign In:
          </div>
        <% end %>
          
      </div>
      </div>
      <div id="main">
        <div id="welcome">
          <% if !logged_in?  %>
            <p>Welcome to <strong>handmade.local</strong>, the best place to find handmade goods by local craftsmen and artists in your area.</p> 
            <p><%= link_to "Create an account", new_user_url %> to automatically see the latest handmade goods in your area.</p>
          <% else %>
          <% end %>
        </div>
        <%=flash[:notice]%>        
        <%= yield %>
      </div>    

    </div>
  </div>

</body>
</html>
